### Usage ###
- To run the server and choose the file to send:
    python(3) ftserv.py file_to_send_to_client (Portnumber hardcoded to 60000)

- To connect with server and specify where to save the file received:
    python(3) ftclient.py server_hostname(ex: localhost) where_to_save_the_file
